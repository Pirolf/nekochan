describe('GameApi', () => {
	const GameApi = require('../../server/game_api');
	describe("#createCats", () => {
		describe("when salmon to cats ratio > 1.5", () => {
			beforeEach(() => {
			});

			it("replace me", () => {
        console.log(GameApi.createCats)
			});
		});
	});
});
